import Link from "next/link";
import Logo from "@/components/Logo";
import { siteConfig } from "@/lib/site-config";
export default function Footer() { return <footer className="border-t border-gray-100 bg-gray-50"><div className="mx-auto max-w-7xl px-6 py-10 text-sm text-gray-500"><nav aria-label="하단 메뉴" className="mb-6 flex flex-wrap gap-5">{[["서비스 안내", "/services/"], ["가격 안내", "/pricing/"], ["회사 소개", "/about/"], ["상담 준비", "/contact/"]].map(([label, href]) => <Link key={href} href={href} className="hover:text-brand">{label}</Link>)}</nav><Logo className="mb-4 h-7 w-auto" /><p>청소는 제대로. 신뢰는 확실하게.</p><p className="mt-2"><a href={`tel:${siteConfig.phoneRaw}`}>전화 상담 {siteConfig.phone}</a></p>{siteConfig.ceo && <p className="mt-3">대표 {siteConfig.ceo}</p>}{siteConfig.businessNumber && <p>사업자등록번호 {siteConfig.businessNumber}</p>}{siteConfig.address && <p>{siteConfig.address}</p>}{siteConfig.phone && <p>{siteConfig.phone}</p>}{siteConfig.email && <p>{siteConfig.email}</p>}<p className="mt-6 text-xs text-gray-400">{siteConfig.copyright}</p></div></footer>; }
